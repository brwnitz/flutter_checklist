name: checklist_master
description: "Aplicação de organização de tarefas."
publish_to: 'none'
version: 1.0.1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+3
  path_provider: ^2.0.2
  flutter_color_picker_plus: ^2.0.1
  flutter_swipe_detector: ^1.0.1
  intl: ^0.19.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/checklist_image.png
